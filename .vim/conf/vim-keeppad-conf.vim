" ##################################
" # Vim-keeppad configuration file #
" ##################################

" Turn on keeppad automatically if this variable is 1.
" Default: 1
let g:keeppad_autopadding = 1

" Clear highlight of the padding(|hl-SignColumn|) if this variable is 1.
" Default: 1
let g:keeppad_no_hl = 1

" Toggle keeppad with the update of the 'number' option.
" Default: 1
let g:keeppad_hook_number_option = 1
